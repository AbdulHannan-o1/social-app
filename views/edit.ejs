<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <div class="bg-zinc-900 w-full min-h-screen text-white p-4">
        <div class="flex justify-between items-center w-full mb-8">
            <a class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" href="/logout">Logout</a>
        </div>
            <h4 class="text-xl font-semibold mb-4 text-center">Edit Your post </h4>
            <form action="/update/<%= post._id %>" method="POST" class="max-w-xl mx-auto p-6 bg-zinc-800 rounded-lg">
                <textarea name="content" placeholder="What's on your mind?" class="w-full p-3 mb-4 bg-zinc-700 border border-zinc-600 rounded-md resize-none focus:outline-none focus:ring-2 focus:ring-blue-500" rows="5"><%= post.content %></textarea>
                <input type="submit" value="update Post" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-4 rounded-md cursor-pointer transition duration-200">
            </form>
    </div>
</body>
</html>
