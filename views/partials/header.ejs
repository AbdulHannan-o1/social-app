<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title><%= typeof title !== 'undefined' ? title : 'Social App' %></title>
</head>
<body class="bg-gradient-to-br from-gray-900 to-black text-white font-sans antialiased">
    <nav class="bg-transparent p-4 shadow-lg">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="text-2xl font-bold text-blue-400 hover:text-blue-300 transition duration-200">SocialApp</a>
            <div class="space-x-4">
                <a href="/profile" class="text-white hover:text-blue-400 transition duration-200">Profile</a>
                <a href="/logout" class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-md transition duration-200 shadow-md">Logout</a>
            </div>
        </div>
    </nav>
    <div class="relative z-10 flex-grow min-h-screen">
